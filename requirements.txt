# Backend runtime dependencies
Flask>=3.1.3  # latest version checked on August 31, 2026
Flask-CORS>=6.0.5

# Automated testing dependencies
pytest>=9.1.1
playwright>=1.62.0

# Built-in modules (no install needed): time, threading, random, datetime, os, webbrowser, json

# Install Python dependencies with: pip install -r requirements.txt
# Install browser binaries for frontend E2E tests with: python -m playwright install chromium
